<main>
	<section class="hero" id="ozel-ders" aria-labelledby="hero-title">
		<div class="hero-accent" aria-hidden="true"></div>
		<div class="hero-container">
			<div class="hero-copy">
				<p class="eyebrow"><span>Deutsch lernen</span> Almancaya güçlü bir başlangıç</p>
				<h1 id="hero-title">Almancayı öğrenin,<br /><em>geleceğinizi</em> büyütün.</h1>
				<p class="intro">
					Hedefinize ve seviyenize özel hazırlanan programlarla Almancayı güvenle konuşun.
					Birebir özel ders veya canlı online kurs seçenekleriyle kendi hızınızda ilerleyin.
				</p>

				<div class="hero-actions">
					<a class="primary-button" href="/seviye-testi">Ücretsiz Seviye Tespiti <span>→</span></a>
					<a class="text-button" href="/ders-alanlari">Programları İncele <span>↗</span></a>
				</div>

				<div class="highlights" aria-label="Kurs özellikleri">
					<div><strong>01</strong><p><b>Uzman eğitmenler</b><span>Deneyimli akademik kadro</span></p></div>
					<div><strong>02</strong><p><b>Esnek program</b><span>Size uygun gün ve saatler</span></p></div>
					<div><strong>03</strong><p><b>Pratik odaklı</b><span>Aktif konuşma ve uygulama</span></p></div>
				</div>
			</div>

			<div class="hero-visual">
				<div class="image-frame">
					<img src="/german-course.jpg" alt="Birlikte Almanca çalışan öğrenciler" width="700" height="820" loading="eager" fetchpriority="high" />
				</div>
				<div class="image-mark" aria-hidden="true"><span></span><span></span><span></span></div>
				<p class="image-caption"><strong>Birebir</strong> ve online eğitim</p>
			</div>
		</div>
	</section>
</main>

<style>
	.hero {
		--red: #dc0b20;
		--red-dark: #a90516;
		--yellow: #f2b900;
		--ink: #1b1b1b;
		position: relative;
		overflow: hidden;
		background:
			linear-gradient(90deg, rgb(255 255 255 / 96%), rgb(255 255 255 / 90%)),
			repeating-linear-gradient(90deg, transparent 0 119px, #f3f1ed 120px);
		font-family: Inter, Arial, Helvetica, sans-serif;
	}

	.hero::before {
		position: absolute;
		top: 0;
		right: 0;
		width: 31%;
		height: 8px;
		background: var(--yellow);
		content: '';
	}

	.hero-accent {
		position: absolute;
		right: -130px;
		bottom: -280px;
		width: 620px;
		height: 620px;
		border: 1px solid rgb(220 11 32 / 10%);
		border-radius: 50%;
		box-shadow: 0 0 0 75px rgb(220 11 32 / 2.5%), 0 0 0 150px rgb(242 185 0 / 2.5%);
	}

	.hero-container {
		position: relative;
		display: grid;
		width: min(1370px, calc(100% - 64px));
		min-height: 650px;
		margin-inline: auto;
		grid-template-columns: minmax(0, 1.04fr) minmax(470px, .96fr);
		align-items: center;
		gap: clamp(55px, 7vw, 110px);
		padding: 70px 0 76px;
		box-sizing: border-box;
		max-width: 100%;
	}

	.hero-copy { position: relative; z-index: 2; }
	.eyebrow { display: flex; margin: 0 0 22px; align-items: center; gap: 14px; color: #625d57; font-size: 11px; font-weight: 700; letter-spacing: .11em; text-transform: uppercase; }
	.eyebrow span { padding: 8px 11px; background: var(--ink); color: var(--yellow); font-size: 9px; letter-spacing: .14em; }

	h1 { max-width: 700px; margin: 0; color: var(--ink); font-family: Georgia, 'Times New Roman', serif; font-size: clamp(48px, 5.2vw, 76px); font-weight: 500; letter-spacing: -.045em; line-height: .98; }
	h1 em { position: relative; color: var(--red); font-weight: 500; }
	h1 em::after { position: absolute; right: 3px; bottom: 0; left: 3px; height: 5px; background: var(--yellow); content: ''; opacity: .85; z-index: -1; }

	.intro { max-width: 620px; margin: 29px 0 0; color: #5e5953; font-size: 16px; line-height: 1.75; }
	.hero-actions { display: flex; margin-top: 34px; align-items: center; gap: 27px; }
	.primary-button { display: inline-flex; min-height: 50px; box-sizing: border-box; align-items: center; gap: 18px; padding: 0 22px; border: 1px solid var(--red); border-radius: 2px; background: var(--red); color: #fff; font-size: 11px; font-weight: 800; letter-spacing: .045em; text-decoration: none; text-transform: uppercase; transition: background 160ms ease; }
	.primary-button span { color: var(--yellow); font-size: 20px; }
	.primary-button:hover { background: var(--red-dark); }
	.text-button { display: inline-flex; align-items: center; gap: 9px; border-bottom: 1px solid #b8b2aa; color: var(--ink); font-size: 11px; font-weight: 800; letter-spacing: .045em; line-height: 31px; text-decoration: none; text-transform: uppercase; }
	.text-button span { color: var(--red); font-size: 15px; }

	.highlights { display: grid; margin-top: 51px; grid-template-columns: repeat(3, 1fr); border-top: 1px solid #dfdcd6; }
	.highlights > div { display: flex; align-items: center; gap: 11px; padding: 21px 18px 0 0; }
	.highlights > div + div { padding-left: 18px; border-left: 1px solid #dfdcd6; }
	.highlights > div > strong { color: var(--red); font-family: Georgia, serif; font-size: 25px; font-weight: 400; }
	.highlights p { display: flex; margin: 0; flex-direction: column; gap: 4px; }
	.highlights b { color: var(--ink); font-size: 11px; text-transform: uppercase; }
	.highlights span { color: #89837c; font-size: 10px; }

	.hero-visual { position: relative; min-width: 0; max-width: 100%; padding: 22px 30px 42px 0; box-sizing: border-box; }
	.hero-visual::before { position: absolute; top: 0; right: 0; width: 78%; height: calc(100% - 42px); background: var(--red); content: ''; }
	.hero-visual::after { position: absolute; right: -18px; bottom: 18px; width: 68%; height: 62%; border: 1px solid rgb(220 11 32 / 26%); content: ''; }
	.image-frame { position: relative; z-index: 1; width: 100%; aspect-ratio: 7 / 5; overflow: hidden; background: #ddd; box-shadow: 0 28px 55px rgb(41 22 16 / 16%); }
	.image-frame::after { position: absolute; inset: 0; background: linear-gradient(180deg, transparent 55%, rgb(17 15 13 / 31%)); content: ''; }
	.image-frame img { display: block; width: 100%; height: 100%; object-fit: cover; object-position: 50% 48%; }
	.image-mark { position: absolute; top: 43px; right: 0; z-index: 2; display: grid; width: 46px; height: 72px; grid-template-rows: repeat(3, 1fr); }
	.image-mark span:nth-child(1) { background: var(--ink); }
	.image-mark span:nth-child(2) { background: var(--red); }
	.image-mark span:nth-child(3) { background: var(--yellow); }
	.image-caption { position: absolute; z-index: 2; right: 0; bottom: 2px; margin: 0; padding: 18px 23px; background: #fff; box-shadow: 0 13px 30px rgb(0 0 0 / 13%); color: var(--ink); font-size: 11px; font-weight: 700; letter-spacing: .07em; text-transform: uppercase; }
	.image-caption strong { color: var(--red); }

	@media (max-width: 1060px) {
		.hero-container { grid-template-columns: 1fr; gap: 70px; padding-top: 65px; }
		.hero-copy { max-width: 760px; }
		.hero-visual { width: min(570px, 100%); margin-inline: auto; }
	}

	@media (max-width: 620px) {
		.hero-container { width: calc(100% - 36px); min-height: auto; padding: 48px 0 64px; gap: 52px; }
		.eyebrow { align-items: flex-start; flex-direction: column; gap: 9px; }
		h1 { font-size: clamp(42px, 13vw, 58px); }
		.intro { margin-top: 23px; font-size: 14px; line-height: 1.7; }
		.hero-actions { align-items: flex-start; flex-direction: column; gap: 14px; }
		.highlights { grid-template-columns: 1fr; margin-top: 38px; }
		.highlights > div { padding: 15px 0; }
		.highlights > div + div { padding-left: 0; border-top: 1px solid #e7e3de; border-left: 0; }
		.hero-visual { min-height: 0; padding: 16px 18px 36px 0; }
		.image-frame { aspect-ratio: 4 / 3; }
		.image-mark { top: 32px; width: 36px; height: 60px; }
		.image-caption { right: 0; bottom: -2px; padding: 15px 17px; font-size: 9px; }
	}
</style>
